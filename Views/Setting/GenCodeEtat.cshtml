
@{
    ViewData["Title"] = "GenCodeEtat";
}

<h2>Build Report code</h2>
<div class="row">
    <form asp-controller="Setting" asp-action="GenCodeEtat" asp-anti-forgery="true" method="post">
        <div class="form-group">  
            <div class="form-group">
                <input  class="form-control" placeholder="Entrez le nom du fichier" type="text" />
            </div>
            <div class="form-group">
                <input  class="form-control" value="rptv_et_hand" placeholder="Entrez le nom de l'état" type="text" />
            </div>
            <div class="form-group">
                <input  class="form-control" placeholder="Entrez le nom de l'état" type="text" />
            </div>
           <button type="submit" class="btn btn-primary">Générer le code</button>

        </div>
    </form>
</div>

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")
}
